import React from 'react';

import classes from './MusicProfile.module.css';

const MusicProfile = (props) => {
  return (
    <div className={classes.profileWrapper}>
      FEEL MUSIC
    </div>
  )
}

export default MusicProfile;
